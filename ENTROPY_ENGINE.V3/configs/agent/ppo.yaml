# @package agent

algorithm: "ppo"

network:
  hidden_dim: 256
  num_layers: 2
  activation: "relu"

ppo:
  learning_rate: 3e-4
  num_steps: 2048
  num_minibatches: 32
  update_epochs: 10
  gamma: 0.99
  gae_lambda: 0.95
  clip_coef: 0.2
  ent_coef: 0.01
  vf_coef: 0.5
  max_grad_norm: 0.5
